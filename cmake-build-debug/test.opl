class A {
	public x: int;
	public y: int;
	constructor(x: int, y: int) {
		this.x = x;
		this.y = y;
	}
}

def main() {
	let x: [A] = [new A(1,2), new A(3,4)];
	let h: int = x[0].x;
	let f: int = x[0].y;
	println(h);
	println(f);
}